defaults:
  - env: polishing
  - controller: position_var #position_var #position_var_sweep
  - task_config: force_control_4
  - algorithm: sac
  - _self_  

root_dir: "./exp"
dir: ${root_dir}/${env.name}/${now:%Y.%m.%d}/${now:%H%M%S}
project: sb3
use_wandb: false
seed: 9
experiment: s_${seed}_r_${task_config.reward_mode}

hydra:
  run:
    dir: ${dir}
